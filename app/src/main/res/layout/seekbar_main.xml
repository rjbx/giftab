<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    android:id="@+id/main_seek_bar_container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

   <LinearLayout
       android:layout_width="match_parent"
       android:layout_height="40dp"
       android:orientation="horizontal"
       android:layout_margin="10dp">

       <TextView
           android:id="@+id/seek_min_label"
           android:layout_width="0dp"
           android:layout_height="match_parent"
           android:layout_weight=".1"
           android:layout_gravity="start"
           android:textSize="@dimen/text_size_paragraph"
           android:text="0.0"/>

       <SeekBar
           android:id="@+id/main_seek_bar"
           android:layout_width="0dp"
           android:layout_height="match_parent"
           android:layout_weight=".8"
           android:layout_gravity="center"/>

       <TextView
           android:id="@+id/seek_max_label"
           android:layout_width="0dp"
           android:layout_height="match_parent"
           android:layout_weight=".1"
           android:layout_gravity="end"
           android:textSize="@dimen/text_size_paragraph"
           android:text="0.1"/>

   </LinearLayout>

    <TextView
        android:id="@+id/seek_progress_display"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textSize="@dimen/text_size_paragraph_title"
        android:gravity="center"/>

</LinearLayout>
